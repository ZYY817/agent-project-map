# YQ — 通用项目地图 Skill

本仓库唯一产品：`skill/yq-map/`——一份遵循 Agent Skills 开放标准（SKILL.md）的通用"项目地图"说明书。

- **装法一**：整个文件夹放进工具的 skills 目录（Claude Code / Codex / Cursor 等），按需触发。
- **装法二**：把 SKILL.md 正文（去掉头部标记）贴进项目的 AGENTS.md 或全局规则，当一段规矩用。

核心卖点：教 AI 在项目里建一张"不会撒谎"的地图（MAP.md），并配一套自检规矩（锚点是否还搜得到、verified 是否被后续改动推翻、链接是否断）。与自动扫描类工具的区别：条目必须标核对状态（verified/assumed/unknown/stale）、含可搜索锚点、地图与代码冲突时以代码为准。已调研竞品：Serena（符号级跳转，28k 星）、cursor-memory-bank（3.1k 星，绑死 Cursor）、各 codemap skill（自动扫图不管过期）——定位交给工具，我们管自动索引产不出的"关系、约束、核对状态"和"地图自检"这个无人区。

## 工作规则

- **先对齐再动笔**：内容设计逐项和用户确认后再写，不提前代劳。
- **白话沟通**：少用术语；用户已授权直接反驳、不迎合。
- **最小充分**：只写当前确认的内容，不为假想需求扩写。

## 历史

2026-09 前曾规划完整客户端（35 份文档）与 dsh 插件方向，均已废弃删除；思路沉淀在 SKILL.md 正文。
